  <hr class="tw:border-secondary-200 tw:dark:tw:border-secondary-600 tw:my-4">
  <div>
    <button type="submit" class="tw:inline-flex tw:items-center tw:gap-2 tw:px-4 tw:py-2 tw:rounded-lg tw:bg-primary-600 tw:text-white hover:tw:bg-primary-700 focus-visible:tw:ring-2 focus-visible:tw:ring-primary-500"><%= Icon(icon: "save", label: t("settings.general.submit")) %> <%= t("settings.general.submit") %></button>
  </div>
