  <hr class="border-secondary-200 dark:border-secondary-500 my-4">
  <div>
    <button type="submit" class="inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-primary-600 text-white hover:bg-primary-700 dark:hover:bg-primary-500 focus-visible:ring-2 focus-visible:ring-primary-500"><%= Icon(icon: "save", label: t("settings.general.submit")) %> <%= t("settings.general.submit") %></button>
  </div>
