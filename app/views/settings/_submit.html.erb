  <hr class="border-secondary-200 dark:border-secondary-500 my-4">
  <div>
    <button type="submit" class="<%= primary_button_class %>"><%= Icon(icon: "save", label: t("settings.general.submit")) %> <%= t("settings.general.submit") %></button>
  </div>
