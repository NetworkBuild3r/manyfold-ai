<div class="max-w-md mx-auto px-4">
  <%= PageTitle title: t(".resend_unlock_instructions") %>

  <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: {method: :post}) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="tabular-form">
      <%= text_input_row f, :email, autofocus: true, autocomplete: "email" %>
      <div>
        <%= f.submit translate(".resend_unlock_instructions"), class: "inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-primary-600 text-white hover:bg-primary-700 focus-visible:ring-2 focus-visible:ring-primary-500" %>
      </div>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
