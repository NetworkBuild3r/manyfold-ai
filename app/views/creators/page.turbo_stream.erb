<%= render layout: "application/browse_window_streams", locals: {
      sentinel_prefix: "creators",
      path_helper: :creators_path,
      loading_label: t("creators.index.loading_more")
    } do %>
  <% @creators.each do |creator| %>
    <%= render creator %>
  <% end %>
<% end %>
