<%# INIT-027/SPEC-009 — unassigned collections tile; markup in application/browse_unassigned. %>
<%= render "application/browse_unassigned",
      name: t(".name"),
      caption: t(".caption"),
      models_params: {collection: ""},
      preview_copy: t("collections.collection.no_preview"),
      spacer: true %>
