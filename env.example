# Optional environment variables (see README.md and config/database.yml).
# MULTIUSER: enable multiuser sign-in as needed for your deployment.
# SOURCE_REPO: Git URL for admin version links (defaults to this repository).
# USAGE_TRACKING_URL: only if you want anonymous usage posts to an endpoint you control.
